const GameBoard = (function() {

  const gameboardArray = [x, x, o, o, o, x, x, x, o];

  const gameboardUI = document.querySelectorAll('.board-field'); 

  const fillBoard = function() {

  };

  return {

  }
}());